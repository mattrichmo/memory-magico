{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://memorymagico.local/schemas/sprint.schema.json",
  "title": "MemoryMagico Sprint Frontmatter",
  "description": "Canonical markdown frontmatter for a sprint page.",
  "type": "object",
  "additionalProperties": true,
  "required": ["id", "kind", "title", "status", "createdAt", "updatedAt"],
  "properties": {
    "id": { "type": "string" },
    "kind": { "const": "sprint" },
    "title": { "type": "string" },
    "status": { "type": "string" },
    "createdAt": { "type": "string" },
    "updatedAt": { "type": "string" }
  }
}
